org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError
org.gradle.parallel=true
org.gradle.caching=false
# The :java module targets Java 25 (net.minestom:minestom 2026.07.12-26.2 ships
# Java-25 bytecode) while Gradle itself runs on JDK 21. Gradle finds a JDK 25 to
# compile against via toolchain auto-provisioning (foojay, in settings.gradle.kts)
# or a locally installed JDK 25. To pin a specific install, set
# org.gradle.java.installations.paths in ~/.gradle/gradle.properties.
